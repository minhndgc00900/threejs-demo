export interface History {
    date: Date;
    value: number;
}

export interface TopProvinceData {
	name: string;
	numberOfFactory: number;
	pollutionLevel: number;
	mainFactor: number;
	mainSector: string;
}